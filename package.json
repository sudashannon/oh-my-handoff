{
  "name": "oh-my-handoff",
  "version": "0.1.0",
  "private": true,
  "description": "OpenCode session handoff system — structured context continuity across sessions",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.4.11"
  }
}
